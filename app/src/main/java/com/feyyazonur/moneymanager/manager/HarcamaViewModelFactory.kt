package com.feyyazonur.moneymanager.manager
/*
import android.app.Application
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.feyyazonur.moneymanager.database.HarcamaDatabaseDao

class HarcamaViewModelFactory(
    private val dataSource: HarcamaDatabaseDao,
    private val application: Application
) : ViewModelProvider.Factory {
    @Suppress("unchecked_cast")
    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(HarcamaViewModel::class.java)) {
            return HarcamaViewModel(dataSource, application) as T
        }
        throw IllegalArgumentException("Unknown ViewModel class")
    }
}*/
